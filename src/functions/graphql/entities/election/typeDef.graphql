  type Election {
    id: String!
    status: String
    result: [Int]
    template: String
    maxVotes: Int,
    maxVoters: Int,
    ttl: Int,
  }

  type ElectionAnalytics {
    votersCount: Int
    votesCount: Int
  }
  
  type Query {
    getElection(id: String!): Election
    getAllElectionWithParent(parent: [String]): [Election]
  }

  type Mutation {
    openElection(votersList: String!, template: String!, ttl: Int, parent: String): String
    closeElection(id: String!): [Int]
    computeVoters(id: String!): ElectionAnalytics
    joinElection(id: String!): String 
  }
